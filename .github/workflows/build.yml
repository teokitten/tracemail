name: Build Tracemail binary
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build binary (python:3.11-slim-bookworm)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            python:3.11-slim-bookworm \
            bash -c "
              pip install --no-cache-dir -r requirements.txt pyinstaller &&
              pyinstaller --onefile --name tracemail --add-data 'templates:templates' app.py
            "
      - name: Download linuxdeploy
        run: |
          curl -fsSL -o linuxdeploy https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod +x linuxdeploy
      - name: Build AppImage
        run: |
          mkdir -p AppDir/usr/bin AppDir/usr/share/icons/hicolor/256x256/apps
          cp dist/tracemail AppDir/usr/bin/tracemail
          cp tracemail.png AppDir/usr/share/icons/hicolor/256x256/apps/tracemail.png
          ./linuxdeploy --appdir AppDir \
            --desktop-file tracemail.desktop \
            --output appimage
      - name: Get short SHA
        id: sha
        run: echo "short=$(echo $GITHUB_SHA | cut -c1-7)" >> $GITHUB_OUTPUT
      - name: Upload release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v-${{ steps.sha.outputs.short }}
          name: Tracemail ${{ steps.sha.outputs.short }}
          files: Tracemail-x86_64.AppImage
          token: ${{ secrets.GITHUB_TOKEN }}
